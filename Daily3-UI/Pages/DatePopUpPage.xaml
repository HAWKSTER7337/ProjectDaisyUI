<?xml version="1.0" encoding="utf-8"?>

<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="Daily3_UI.Pages.DatePopUpPage">
    <Border
        Stroke="{StaticResource Black}"
        StrokeThickness="2"
        StrokeShape="RoundRectangle 10,10,10,10"
        BackgroundColor="{StaticResource Secondary}"
        VerticalOptions="Center"
        HorizontalOptions="Center">
    <Grid Padding="10"
          VerticalOptions="Center"
          HorizontalOptions="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label
            Grid.Row="0"
            TextColor="{StaticResource White}"
            Text="Select a Date"
            FontSize="30"
            HorizontalTextAlignment="Center"/>
        
        <Grid
            Padding="6"
            Margin="10,10,10,10"
            Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Label
                Grid.Row="0"
                Grid.Column="0"
                Text="Month"
                Padding="5"
                Margin="5"
                TextColor="{StaticResource White}"
                FontSize="25"/>
            
            <Border
                Grid.Row="1"
                Grid.Column="0"
                StrokeShape="RoundRectangle 10,0,10,0"
                BackgroundColor="{StaticResource White}">
                <Picker
                    Margin="5"
                    ItemsSource="{Binding Months}"
                    SelectedItem="{Binding MonthString}"
                    Style="{StaticResource Picker-Ball}"
                    TextColor="{StaticResource Black}"
                    HorizontalTextAlignment="Center"
                    FontSize="35"/>
            </Border>
            
            <Label
                Grid.Row="0"
                Grid.Column="1"
                Text="Day"
                Padding="5"
                Margin="5"
                TextColor="{StaticResource White}"
                FontSize="25"/>
            
            <Border
                Grid.Row="1"
                Grid.Column="1"
                StrokeShape="RoundRectangle 0,10,0,10"
                BackgroundColor="{StaticResource White}">
                <Picker
                    Margin="5"
                    Focused="DayPickerFocused"
                    ItemsSource="{Binding DaysInMonth}"
                    SelectedItem="{Binding DayString}"
                    Style="{StaticResource Picker-Ball}"
                    TextColor="{StaticResource Black}"
                    HorizontalTextAlignment="Center"
                    FontSize="35"/>
            </Border>
            
        </Grid>
        
        <Button
            Grid.Row="2"
            Style="{StaticResource Daily-Button}"
            Text="Exit"
            Clicked="OnExitClicked"/>
        
    </Grid>
    </Border>
</toolkit:Popup>